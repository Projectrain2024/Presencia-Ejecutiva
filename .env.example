# Railway provee DATABASE_URL automáticamente al conectar PostgreSQL
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Contraseña de acceso al hub (cambia esto antes de desplegar)
HUB_PASSWORD=lhh2026

# Clave para cifrar sesiones (genera una cadena aleatoria larga)
SESSION_SECRET=cambia-esto-por-una-cadena-aleatoria-segura

NODE_ENV=production
